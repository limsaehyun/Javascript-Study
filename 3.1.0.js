const intNUm = 10; // Number
const floatNum = 0.1;

const singleQuoteStr = "single quote string" // String
const doubleQuoteStr = "double quote string"
const singleChar = "a"

const boolVar = true;

let emptyVar; // Undefined

const nullVar = null; // null

console.log (
    typeof intNUm,
    typeof floatNum,
    typeof singleQuoteStr,
    typeof doubleQuoteStr,
    typeof singleChar,
    typeof boolVar,
    typeof emptyVar,
    typeof nullVarn
)